# Usage

1. Install docker
2. Enable dnpm, dnpx (docker-npm, docker-npx) aliases:
    ```
    source aliases.sh
    ```
3. Install packages:
    ```
    dnpm install
    ```
4. Run 'start' script (for webpack-dev-server + hotreload):
    ```
    dnpm run start
    ```
